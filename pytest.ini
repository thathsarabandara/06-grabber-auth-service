[pytest]
# Environment variables injected before any test collection.
# These ensure pydantic-settings can instantiate Settings() even without a .env file.
# Used by CI (Jenkins / GitHub Actions) where no .env is present.
env =
    DATABASE_URL=sqlite:///./test_temp.db
    SECRET_KEY=test-secret-key-for-ci
    ALGORITHM=HS256
